# GitHub logins allowed to approve deployments to pro (one per line, case-insensitive).
# Only their approval of a "Deploy <tag> to PRO" pull request counts (see deploy-pro.yml).
# This file lives under .github/, so it is only changeable by the code owners of that folder.
# The author of the pull request can never approve it.
#
# example-login
X637800-sgt_sangroup
